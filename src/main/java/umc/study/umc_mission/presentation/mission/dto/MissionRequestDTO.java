package umc.study.umc_mission.presentation.mission.dto;

/**
 * Mission(미션) 도메인 요청 DTO 모음.
 *
 * <p>현재는 내 미션·지역 미션 조회 모두 PathVariable + QueryParam만 사용하므로
 * Body 요청 DTO는 비어 있다. 추후 미션 도전/완료 처리 API에서 사용될 예정이라
 * 네임스페이스 클래스만 미리 둔다.</p>
 */
public class MissionRequestDTO {
}
